import React from "react";

const StickyFooter = ({ isDark }) => {
  return (
    <footer className="fixed bottom-0 left-0 right-0 bg-gray-800 text-white py-2 flex justify-around items-center shadow-md z-50">
      dasd
    </footer>
  );
};

export default StickyFooter;
